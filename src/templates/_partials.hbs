{{!--
  Shared styling block. This file isn't loaded automatically — it's here as a
  reference for the per-report templates. Each report template duplicates the
  small <style> block because Handlebars partials would otherwise require
  additional registration plumbing in pdfService.
--}}
